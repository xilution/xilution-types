export interface IOrganization {
  id: string;
  name: string;
}

export interface IClient {
  id: string;
  name: string;
}

export interface IUser {
  id: string;
  firstName: string;
  lastName: string;
}
